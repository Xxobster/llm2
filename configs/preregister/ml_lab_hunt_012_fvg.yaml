# ML lab hunt 012 — Fair Value Gap (FVG) signals
# RESEARCH_ONLY. Frozen before any hunt-012 result was viewed.
generation_id: ml_lab_hunt_012_fvg
status: OPEN
readiness_max: RESEARCH_ONLY
frozen_at_utc: "2026-09-03T15:40:00Z"
fold_geometry: v2
failure_is_success: true

hypothesis: >
  Fair Value Gap (FVG): three-bar price imbalance (high[t-2] < low[t] bullish;
  high[t] < low[t-2] bearish). Eight causal ideas: pullback into a prior
  bullish FVG (long); pullback into a prior bearish FVG (short); gap-and-go
  follow on fresh gap; FVG in choppy Efficiency Ratio market (fade the impulse);
  price touches gap then closes back through (mitigated reversal); age-filtered
  pullback; large gap (> 1.5× Average True Range) follow; bullish FVG
  confluence with 20-bar low intact. Gap must span ≥ 0.30× Average True Range
  to count. Maker 0.02% resting legs. EXEC-021 one-minute fill clock.
  Inner screen strictly before 2022-01-01.

do_not:
  - peep FORWARD_LOCKBOX_START
  - deploy
  - stamp Shadow-Ready from this inner screen
  - widen Average-True-Range to hide entry-bar exits
  - search the gap-size / age / pullback thresholds on profit factor

selection_window: "strictly before 2022-01-01"
universe:
  symbols: [BTCUSDT, ETHUSDT, SOLUSDT]
  event_study_timeframes: [15m, 1h, 4h]
  tradesim_timeframes: [15m, 1h, 4h]

cost_floor: 0.0011
max_tradesim_arms: 8
work_bars: {15m: 4, 1h: 3, 4h: 2}
max_hold_bars: {15m: 24, 1h: 16, 4h: 8}
k_sl: 1.5
tp_ratio: 1.5
sl_cap: 0.030
maker_first_costs: true

screen_gates:
  min_skill_n: 80
  min_directional_acc: 0.51
  min_mean_signed: 0.0015
  min_trades: 80
  min_trades_per_month: 2.0
  max_trades_per_month: 90.0
  min_profit_factor: 1.20
  max_entry_bar_exit_rate: 0.25
  min_sharpe_annualised: 0.50

leakage_audit:
  builder: llm2.ml_lab.idea_catalog_i.build_features_for_guard

preregister_sha256_at_run: dbfbcb511b1a93847b18cf342d7e1c7b16f2591b06d4a7422ebc9a7589d35c3a

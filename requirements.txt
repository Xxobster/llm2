# LLM2 predictability laboratory — install into .venv (Python 3.12)
# CUDA torch: pip install torch==2.6.0+cu124 --index-url https://download.pytorch.org/whl/cu124
numpy==2.2.6
pandas==2.3.3
scipy==1.15.2
scikit-learn>=1.4
statsmodels>=0.14
pyarrow>=15
lightgbm>=4.0
catboost>=1.2
xgboost>=2.0
joblib
pyyaml
tqdm
matplotlib
seaborn
finplot>=1.9
pytest>=8
pandas_ta
arch
httpx
# Editable shared engines:
# pip install -e C:\projects\botsgeneral\packages\tradesim[plot,conformance]
# pip install -e C:\projects\botsgeneral\packages\leakage[dev]
# pip install -e C:\projects\botsgeneral
# Optional foundation (may conflict with torch pin):
# chronos-forecasting, timesfm, neuralforecast
